# Converge — local env template (Phase 1 skeleton)
# Copy to `.env.local` and fill when wiring providers.

# Supabase (project: converge / cxhfmsnkcketqdlcqdic)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Auth0 (Phase 1.3 — not wired yet)
AUTH0_SECRET=
AUTH0_BASE_URL=http://localhost:3000
AUTH0_ISSUER_BASE_URL=
AUTH0_CLIENT_ID=
AUTH0_CLIENT_SECRET=
